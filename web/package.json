{
  "name": "eurorack-case-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The Eurorack case model in replicad (OpenCascade in the browser).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "replicad": "1.0.1",
    "replicad-opencascadejs": "1.0.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.4.3",
    "vitest": "^2.1.0"
  }
}
